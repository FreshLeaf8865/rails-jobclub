- set_meta_tags title: "What are your skills?"

= render "nav"

.container.mt-3.mb-5
  .row.justify-content-center
    .col-lg-6.col-md-8.col-sm-10
      .progress.mb-4  
        .progress-bar{"aria-valuemax" => "100", "aria-valuemin" => "0", "aria-valuenow" => "25", :role => "progressbar", :style => "width: 80%; height: 5px;"}

      %h1 🔥 What are your skills?

      = form_for @user, url: wizard_path, method: :put do |f|
        = f.hidden_field :email
        #user_skills
          = f.fields_for :user_skills do |user_skill|
            = render 'user_skill_fields', f: user_skill
          .links
            = link_to_add_association f, :user_skills, class: "btn btn-info btn-lg btn-block" do
              %i.fa.fa-plus.mr-1
              Add Skill
            %hr
            
        = f.submit "Next", class: "btn btn-primary btn-lg btn-block"
