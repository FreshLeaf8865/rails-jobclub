.container.content_container
  %h1 
    = title "Badges"
    %small
      = link_to 'New', new_badge_path, class: "btn btn-primary btn-sm" if policy(Badge).new?
  %p
    %small
      = pluralize(Badge.all.count, "badge")

  .badges.mt-4
    = render @badges

  = paginate @badges
